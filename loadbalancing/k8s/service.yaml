apiVersion: v1
kind: Service
metadata:
  name: uim-loadbalancing-service
  labels:
    app: uim-loadbalancing-service
spec:
  type: ClusterIP
  selector:
    app: uim-loadbalancing-service
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP

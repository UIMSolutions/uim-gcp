apiVersion: apps/v1
kind: Deployment
metadata:
  name: uim-loadbalancing-service
  labels:
    app: uim-loadbalancing-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: uim-loadbalancing-service
  template:
    metadata:
      labels:
        app: uim-loadbalancing-service
    spec:
      containers:
      - name: service
        image: localhost/uim-loadbalancing-service:0.1.0
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
        env:
        - name: PORT
          value: "8080"
        readinessProbe:
          httpGet:
            path: /readyz
            port: 8080
          initialDelaySeconds: 2
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "256Mi"

apiVersion: v1
kind: Service
metadata:
  name: uim-appengine-service
  labels:
    app: uim-appengine-service
spec:
  type: ClusterIP
  selector:
    app: uim-appengine-service
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
